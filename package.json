{
  "name": "chainattend",
  "version": "1.0.0",
  "description": "Blockchain Student Attendance System",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "2.22.0",
    "@nomicfoundation/hardhat-toolbox": "4.0.0"
  },
  "dependencies": {
    "dotenv": "16.4.5",
    "ethers": "6.7.0"
  }
}
